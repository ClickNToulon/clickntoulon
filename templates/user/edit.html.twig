{% extends 'base.html.twig' %}

{% block title 'Modifier mes informations - TouSolidaires' %}

{% block body %}
    {% include 'navbar.html.twig' with {'active': 'user'} %}
    {% include 'user/_header.html.twig' %}
    {# <div class="toast-container position-absolute p-3">
        {% for message in app.flashes('success') %}
            <div role="alert" class="fr-alert fr-alert--success show" id="alert">
                <p>{{ message }}</p>
            </div>
            <script>
                const alert = document.querySelector("#alert")
                console.log(alert)
                setTimeout(() => {
                    alert.classList.remove('show')
                }, 10000);
            </script>
        {% endfor %}
        {% for message in app.flashes('warning') %}
            <div role="alert" class="fr-alert fr-alert--danger show" id="alert">
                <p>{{ message }}</p>
            </div>
            <script>
                const alert = document.querySelector("#alert")
                console.log(alert)
                setTimeout(() => {
                    alert.classList.remove('show')
                }, 10000);
            </script>
        {% endfor %}
    </div>#}
    <section class="text-gray-400 body-font">
        <div class="container px-5 py-12 pt-6 mx-auto">
            <div class="flex justify-center flex-wrap -m-4">
                {% include 'user/_tabs.html.twig' with {active: 'edit'} %}
            </div>
        </div>
        <div class="container px-5 mx-auto sm:max-w-7xl pb-12 max-w-full">
            <div class="w-full">
                <div class="block md:grid md:grid-cols-3 md:gap-6">
                    <div class="md:col-span-1">
                        <div class="px-4 sm:px-0">
                            <h3 class="text-lg font-medium leading-6 text-black dark:text-white inline-flex items-center">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5.121 17.804A13.937 13.937 0 0112 16c2.5 0 4.847.655 6.879 1.804M15 10a3 3 0 11-6 0 3 3 0 016 0zm6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                                </svg>
                                <span class="ml-1">Votre profil</span>
                            </h3>
                            <p class="mt-1 text-sm text-gray-600 dark:text-gray-300">
                                Tous les champs doivent être remplis, sauf pour l'adresse mail qui est facultatif
                            </p>
                        </div>
                    </div>
                    <div class="mt-5 md:mt-0 md:col-span-2">
                        {{ form_start(form_profile) }}
                            <div class="shadow sm:overflow-hidden rounded-2xl">
                                <div class="px-4 py-5 bg-gray-800 space-y-6 sm:p-6 rounded-t-2xl">
                                    <div class="grid grid-cols-2 gap-6">
                                        <div class="col-span-2 sm:col-span-1">
                                            <label for="update_user_form_fullName" class="block text-sm font-medium text-gray-600 dark:text-white">
                                                {{ form_label(form_profile.fullName) }}
                                            </label>
                                            <div class="mt-1 flex rounded-md shadow-sm">
                                                {{ form_widget(form_profile.fullName) }}
                                            </div>
                                        </div>
                                        <div class="col-span-2 sm:col-span-1">
                                            <label for="update_user_form_email" class="block text-sm font-medium text-gray-600 dark:text-white">
                                                {{ form_label(form_profile.email) }}
                                            </label>
                                            <div class="mt-1 flex rounded-md shadow-sm">
                                                {{ form_widget(form_profile.email) }}
                                            </div>
                                        </div>
                                    </div>
                                    <div>
                                        <label for="update_user_form_address" class="block text-sm font-medium text-gray-600 dark:text-white">
                                            {{ form_label(form_profile.address) }}
                                        </label>
                                        <div class="mt-1">
                                            {{ form_widget(form_profile.address) }}
                                        </div>
                                    </div>
                                    <div class="grid grid-cols-2 gap-6">
                                        <div class="col-span-2 sm:col-span-1">
                                            <label for="update_user_form_postal_code" class="block text-sm font-medium text-gray-600 dark:text-white">
                                                {{ form_label(form_profile.postal_code) }}
                                            </label>
                                            <div class="mt-1 flex rounded-md shadow-sm">
                                                {{ form_widget(form_profile.postal_code) }}
                                            </div>
                                        </div>
                                        <div class="col-span-2 sm:col-span-1">
                                            <label for="update_user_form_city" class="block text-sm font-medium text-gray-600 dark:text-white">
                                                {{ form_label(form_profile.city) }}
                                            </label>
                                            <div class="mt-1 flex rounded-md shadow-sm">
                                                {{ form_widget(form_profile.city) }}
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                <div class="px-4 py-3 bg-gray-800 text-right sm:px-6 rounded-b-2xl">
                                    <button type="submit" class="inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-yellow-600 hover:bg-yellow-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-yellow-500">
                                        Sauvegarder les changements
                                    </button>
                                </div>
                            </div>
                        {{ form_end(form_profile) }}
                    </div>
                </div>
            </div>

            <div class="block" aria-hidden="true">
                <div class="py-5">
                    <div class="border-t border-gray-600 dark:border-gray-200"></div>
                </div>
            </div>

            <div class="mt-0">
                <div class="md:grid md:grid-cols-3 md:gap-6">
                    <div class="md:col-span-1">
                        <div class="px-4 sm:px-0">
                            <h3 class="text-lg font-medium leading-6 text-black dark:text-white inline-flex items-center">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
                                </svg>
                                <span class="ml-1">Votre mot de passe</span>
                            </h3>
                        </div>
                    </div>
                    <div class="mt-5 md:mt-0 md:col-span-2 w-full md:w-auto">
                        {{ form_start(form_password) }}
                            <div class="shadow overflow-hidden rounded-2xl">
                                <div class="px-4 py-5 bg-white dark:bg-gray-800 sm:p-6">
                                    <div class="grid grid-cols-6 gap-6">
                                        <div class="col-span-6 sm:col-span-3">
                                            <label for="update_password_form_password_first" class="block text-sm font-medium text-gray-600 dark:text-white">
                                                {{ form_label(form_password.password.first) }}
                                            </label>
                                            <div class="mt-1 flex rounded-md shadow-sm">
                                                {{ form_widget(form_password.password.first) }}
                                            </div>
                                        </div>

                                        <div class="col-span-6 sm:col-span-3">
                                            <label for="update_password_form_password_first" class="block text-sm font-medium text-gray-600 dark:text-white">
                                                {{ form_label(form_password.password.second) }}
                                            </label>
                                            <div class="mt-1 flex rounded-md shadow-sm">
                                                {{ form_widget(form_password.password.second) }}
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                <div class="px-4 py-3 bg-white dark:bg-gray-800 text-right sm:px-6">
                                    <button type="submit" class="inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-yellow-600 hover:bg-yellow-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-yellow-500">
                                        Sauvegarder le mot de passe
                                    </button>
                                </div>
                            </div>
                        {{ form_end(form_password) }}
                    </div>
                </div>
            </div>

            <div class="block" aria-hidden="true">
                <div class="py-5">
                    <div class="border-t border-gray-200"></div>
                </div>
            </div>

            <div class="mt-0">
                <div class="md:grid md:grid-cols-3 md:gap-6">
                    <div class="md:col-span-1">
                        <div class="px-4 sm:px-0">
                            <h3 class="text-lg font-medium leading-6 text-black dark:text-white inline-flex items-center">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z" />
                                </svg>
                                <span class="ml-1">Votre photo de profil</span>
                            </h3>
                        </div>
                    </div>
                    <div class="mt-5 md:mt-0 md:col-span-2">
                        {{ form_start(form_avatar) }}
                            <div class="shadow overflow-hidden rounded-2xl">
                                <div class="px-4 py-5 bg-white dark:bg-gray-800 space-y-6 sm:p-6">
                                    <div>
                                        <label class="block text-sm font-medium text-gray-600 dark:text-white">
                                            {{ form_label(form_avatar.avatar) }}
                                        </label>
                                        <div class="mt-1 inline-flex items-center pr-4 sm:pr-6 w-full">
                                            <span class="inline-block h-12 w-14 sm:h-20 sm:w-24 rounded-full overflow-hidden bg-gray-400 dark:bg-gray-900">
                                                {% if user.avatar is not null %}
                                                    <img src="{{ asset('uploads/users/' ~ user.avatar) }}" alt="" class="h-full w-full">
                                                {% else %}
                                                    <img src="{{ asset('images/user.png') }}" alt="" class="h-full w-full">
                                                {% endif %}
                                            </span>
                                            {{ form_widget(form_avatar.avatar) }}
                                        </div>
                                    </div>
                                </div>
                                <div class="px-4 py-3 bg-white dark:bg-gray-800 text-right sm:px-6">
                                    <button type="submit" class="inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-yellow-600 hover:bg-yellow-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-yellow-500">
                                        Sauvegarder la photo de profil
                                    </button>
                                </div>
                            </div>
                        {{ form_end(form_avatar) }}
                    </div>
                </div>
            </div>

            <div class="block" aria-hidden="true">
                <div class="py-5">
                    <div class="border-t border-gray-200"></div>
                </div>
            </div>

            <div class="mt-0">
                <div class="md:grid md:grid-cols-3 md:gap-6">
                    <div class="md:col-span-1">
                        <div class="px-4 sm:px-0">
                            <h3 class="text-lg font-medium leading-6 text-red-500 inline-flex items-center">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7a4 4 0 11-8 0 4 4 0 018 0zM9 14a6 6 0 00-6 6v1h12v-1a6 6 0 00-6-6zM21 12h-6" />
                                </svg>
                                <span class="ml-1">Supprimer votre compte</span>
                            </h3>
                        </div>
                    </div>
                    <div class="mt-5 md:mt-0 md:col-span-2">
                        {{ form_start(form_delete) }}
                        <div class="shadow overflow-hidden rounded-2xl">
                            <div class="px-4 py-5 bg-white dark:bg-gray-800 space-y-6 sm:p-6">
                                <div class="text-center flex items-center flex-col">
                                    <p class="mt-2 text-black dark:text-white font-medium">Vous n'&ecirc;tes pas satisfait du contenu propos&eacute;
                                        sur le site ?<br>
                                        Ou vous souhaitez supprimer toutes les informations associ&eacute;es à votre
                                        compte ?
                                    </p>
                                    <p class="mt-2 text-black dark:text-white font-medium">Cette fonctionnalit&eacute; va supprimer toutes les donn&eacute;es
                                        relatives &agrave; votre profil, ainsi que toutes vos commandes pass&eacute;es
                                        sur le site.
                                    </p>
                                    {{ form_row(form_delete.id) }}
                                </div>
                            </div>
                            <div class="px-4 py-3 bg-white dark:bg-gray-800 text-right sm:px-6">
                                <button type="submit" class="inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500">
                                    Supprimer votre compte
                                </button>
                            </div>
                        </div>
                        {{ form_end(form_delete) }}
                    </div>
                </div>
            </div>
        </div>
    </section>
    {% include 'footer.html.twig' %}
{% endblock %}