<!DOCTYPE html>
<html lang="fr" class="bg-darkblue-100 dark:bg-darkblue-900">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}{% endblock %}</title>
        <meta name="description" content="ClickNToulon est une plateforme de click&collect locale basée sur Toulon, qui permet d'apporter une source de revenus supplémentaires aux commerçants">
        <meta name="robots" content="all">
        <link rel="icon" href="{{ asset('images/icon.svg') }}">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body class="relative">
        <div class="relative">
            <div id="cookies-eu-banner" class="mt-5 text-white" style="display: none;" data-wait-remove="250">
                <div class="flex items-center justify-center font-bold flex-col">
                    <p>En continuant votre visite sur ce site, vous acceptez l'utilisation des cookies nécessaires pour les statistiques des visites.</p>
                    <div class="mt-3 space-x-4">
                        <a href="{{ path('about_cgu') }}#cookies" id="cookies-eu-more" class="w-full sm:w-auto inline-flex justify-center rounded-full border-2 border-blue-900 shadow-sm px-4 py-2 mt-3 sm:mt-0 sm:ml-3 hover:bg-blue-900 text-blue-900 hover:text-white dark:text-white text-base font-bold">
                            En savoir plus
                        </a>
                        <button id="cookies-eu-reject" class="w-full sm:w-auto inline-flex justify-center rounded-full border-2 border-red-600 shadow-sm px-4 py-2 sm:ml-3 hover:bg-red-600 text-base font-bold text-white">
                            Rejeter
                        </button>
                        <button id="cookies-eu-accept" class="inline-flex justify-center py-2 px-4 shadow-sm text-base font-bold rounded-full text-white bg-blue-600 hover:bg-blue-500 focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 focus:ring-offset-white dark:focus:ring-offset-darkblue-800">
                            Accepter
                        </button>
                    </div>
                </div>
            </div>
        </div>
        {% block body %}{% endblock %}
        <script src="{{ asset('js/cookies-eu-banner.min.js') }}"></script>
        <script>
            const cookiesBanner = new CookiesEuBanner(function () {
                console.log('Cookie banner loaded')
            }, true, true);
            cookiesBanner.addClickListener(document.getElementById('cookies-eu-accept'), function () {
                cookiesBanner.setConsent(true);
                cookiesBanner.removeBanner();
            });
            cookiesBanner.addClickListener(document.getElementById('cookies-eu-reject'), function () {
                cookiesBanner.setConsent(false);
                cookiesBanner.removeBanner();
            });
        </script>
    </body>
</html>
