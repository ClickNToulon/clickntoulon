{% extends 'base.html.twig' %}

{% block title 'Administration - TouSolidaires' %}

{% block body %}
    {% include 'admin/navbar.html.twig' with {'active': 'home'} %}
    <div class="fr-container fr-container--fluid pt-5">
        <div class="d-flex">
            <div class="w-50">
                {{ render_chart(chart) }}
                <h3 class="text-center mt-2">Données sur les utilisateurs, boutiques et les produits</h3>
            </div>
            <div class="w-50">
                {{ render_chart(chart_other) }}
                <h3 class="text-center mt-2">Données sur les paniers et les commandes</h3>
            </div>
        </div>
    </div>
{% endblock %}
