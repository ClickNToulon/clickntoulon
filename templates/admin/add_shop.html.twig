{% extends 'base.html.twig' %}

{% block title 'Créer une boutique - Administration - TouSolidaires' %}

{% block body %}
    <div class="d-flex flex-row">
        {% include 'admin/sidebar.html.twig' with {'active': 'shops'} %}
        <div class="fr-container fr-container--fluid">
            {{ form_start(form, {attr: {class: 'user-modify'}}) }}
            <h3 class="user-modify-title"><i class="bi-shop-window"></i> Informations de la nouvelle boutique</h3>
            <div class="forms form-modify">
                <div class="fr-alert fr-alert--info fr-alert--sm">
                    <p>Tous les champs doivent être remplis, sauf pour l'adresse mail qui est facultatif</p>
                </div>
                <div class="row first">
                    <div class="col-12 form-row">
                        {{ form_row(form.name) }}
                        {{ form_row(form.tag) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-12 form-row">
                        {{ form_row(form.email) }}
                        {{ form_row(form.phone) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-12 form-row form-address">
                        {{ form_row(form.description) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-12 form-row form-address">
                        {{ form_row(form.address) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-12 form-row">
                        {{ form_row(form.postal_code) }}
                        {{ form_row(form.city) }}
                    </div>
                </div>
                <div class="row">
                    <div class="col-12 form-row">
                        {{ form_row(form.cover) }}
                    </div>
                </div>
            </div>
            <div class="button-submit">
                <button type="submit" class="btn btn-form" name="add_shop">Créer la boutique</button>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}