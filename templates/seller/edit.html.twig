{% extends 'base.html.twig' %}

{% block title 'Modifier les informations de ma boutique - ClickNToulon' %}

{% block body %}
    {% include 'navbar.html.twig' with {active: 'my-shop'} %}
    {% include 'seller/_header.html.twig' %}
    <section class="text-gray-400 body-font">
        <div class="sm:container px-5 py-6 mx-auto">
            <div class="flex justify-center flex-wrap -m-4 mb-4">
                {% include 'seller/_tabs.html.twig' with {active: 'edit'} %}
            </div>
        </div>
        <div class="container px-5 mx-auto sm:max-w-7xl pb-12 max-w-full">
            <div class="w-full">
                <div class="block md:grid md:grid-cols-3 md:gap-6">
                    <div class="md:col-span-1">
                        <div class="px-4 sm:px-0">
                            <h3 class="text-2xl font-bold uppercase text-black inline-flex items-center">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5.121 17.804A13.937 13.937 0 0112 16c2.5 0 4.847.655 6.879 1.804M15 10a3 3 0 11-6 0 3 3 0 016 0zm6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
                                </svg>
                                <span class="ml-1">Votre boutique</span>
                            </h3>
                            <p class="mt-1 text-base text-black">
                                Tous les champs doivent être remplis, sauf pour l'adresse mail qui est facultatif
                            </p>
                        </div>
                    </div>
                    <div class="mt-5 md:mt-0 md:col-span-2">
                        {{ form_start(form_update) }}
                        <div class="shadow-2xl sm:overflow-hidden rounded-lg">
                            <div class="px-4 py-5 bg-white space-y-6 sm:p-6 rounded-t-lg">
                                <div class="grid grid-cols-2 gap-6">
                                    <div class="col-span-2 sm:col-span-1">
                                        {{ form_label(form_update.name) }}
                                        <div class="mt-1 flex rounded-md shadow-sm">
                                            {{ form_widget(form_update.name) }}
                                        </div>
                                    </div>
                                    <div class="col-span-2 sm:col-span-1">
                                        {{ form_label(form_update.tag) }}
                                        <div class="mt-1 flex rounded-md shadow-sm">
                                            {{ form_widget(form_update.tag) }}
                                        </div>
                                    </div>
                                </div>
                                <div class="grid grid-cols-2 gap-6">
                                    <div class="col-span-2 sm:col-span-1">
                                        {{ form_label(form_update.email) }}
                                        <div class="mt-1 flex rounded-md shadow-sm">
                                            {{ form_widget(form_update.email) }}
                                        </div>
                                    </div>
                                    <div class="col-span-2 sm:col-span-1">
                                        {{ form_label(form_update.phone) }}
                                        <div class="mt-1 flex rounded-md shadow-sm">
                                            {{ form_widget(form_update.phone) }}
                                        </div>
                                    </div>
                                </div>
                                <div>
                                    {{ form_label(form_update.description) }}
                                    <div class="mt-1">
                                        {{ form_widget(form_update.description) }}
                                    </div>
                                </div>
                                <div>
                                    {{ form_label(form_update.address) }}
                                    <div class="mt-1">
                                        {{ form_widget(form_update.address) }}
                                    </div>
                                </div>
                                <div class="grid grid-cols-2 gap-6">
                                    <div class="col-span-2 sm:col-span-1">
                                        {{ form_label(form_update.postalCode) }}
                                        <div class="mt-1 flex rounded-md shadow-sm">
                                            {{ form_widget(form_update.postalCode) }}
                                        </div>
                                    </div>
                                </div>
                                <div class="grid grid-cols-5 gap-1">
                                    {% for payment in payments %}
                                    <div class="col-span-2 lg:col-span-1">
                                        <div class="inline-flex items-start">
                                            <div class="mt-2">
                                                <div>
                                                    <label class="inline-flex items-center align-middle">
                                                        <input type="checkbox" class="rounded border-gray-300 text-yellow-500 shadow-sm focus:border-yellow-300 focus:ring focus:ring-offset-0 focus:ring-yellow-200 focus:ring-opacity-50" name="payment[]" value="{{ payment.id }}" id="payment_{{ payment.id }}" {% if payment in shop.payments %}checked{% endif %}>
                                                        <span class="ml-2 text-black font-semibold">{{ payment.name }}</span>
                                                    </label>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    {% endfor %}
                                </div>
                            </div>
                            <div class="px-4 py-3 bg-white text-right sm:px-6 rounded-b-lg">
                                <button type="submit" class="inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-base font-semibold rounded-md text-white bg-yellow-500 hover:bg-yellow-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-yellow-500 focus:ring-offset-white">
                                    Sauvegarder les changements
                                </button>
                            </div>
                        </div>
                        {{ form_end(form_update) }}
                    </div>
                </div>
            </div>
            <div class="block" aria-hidden="true">
                <div class="py-5">
                    <div class="border-t border-gray-600"></div>
                </div>
            </div>
            <div class="mt-0">
                <div class="md:grid md:grid-cols-3 md:gap-6">
                    <div class="md:col-span-1">
                        <div class="px-4 sm:px-0">
                            <h3 class="text-2xl font-bold uppercase text-black inline-flex items-center">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                                </svg>
                                <span class="ml-1">Vos horaires</span>
                            </h3>
                        </div>
                    </div>
                    <div class="mt-5 md:mt-0 md:col-span-2 w-full md:w-auto">
                        <form action="" method="post" name="test" id="test">
                            <div class="shadow-2xl overflow-hidden rounded-lg">
                                <div class="px-4 py-5 bg-white space-y-6 sm:p-6 rounded-t-lg">
                                    <div class="flex flex-wrap items-center justify-center p-1 rounded-lg bg-white shadow-xl w-full sm:w-auto">
                                        <a class="tab w-full sm:w-auto cursor-pointer" id="lundiTab" onclick="changeTab('lundi', 'lundiTab')">Lundi</a>
                                        <a class="tab w-full sm:w-auto cursor-pointer" id="mardiTab" onclick="changeTab('mardi', 'mardiTab')">Mardi</a>
                                        <a class="tab w-full sm:w-auto cursor-pointer" id="mercrediTab" onclick="changeTab('mercredi', 'mercrediTab')">Mercredi</a>
                                        <a class="tab w-full sm:w-auto cursor-pointer" id="jeudiTab" onclick="changeTab('jeudi', 'jeudiTab')">Jeudi</a>
                                        <a class="tab w-full sm:w-auto cursor-pointer" id="vendrediTab" onclick="changeTab('vendredi', 'vendrediTab')">Vendredi</a>
                                        <a class="tab w-full sm:w-auto cursor-pointer" id="samediTab" onclick="changeTab('samedi', 'samediTab')">Samedi</a>
                                        <a class="tab w-full sm:w-auto cursor-pointer" id="dimancheTab" onclick="changeTab('dimanche', 'dimancheTab')">Dimanche</a>
                                    </div>
                                    <div class="grid grid-cols-2 gap-2 gap-y-2 hidden" id="lundi">
                                        <div class="col-span-2 flex flex-row">
                                            <label class="block text-base font-semibold text-black">
                                                Lundi
                                            </label>
                                        </div>
                                        {% set a = 1 %}
                                        {% for openingHour in openingHours %}
                                            {% if openingHour.day == 1 %}
                                                <div class="col-span-2 sm:col-span-1">
                                                    <label for="lundiTimeOpen{{ a }}" class="text-black">Ouverture</label>
                                                    <div class="flex rounded-md shadow-sm">
                                                        <input type="time" name="lundiTimeOpen{{ a }}" {% if openingHour.start %}value="{{ openingHour.start |format_time(locale='fr') }}"{% endif%} id="lundiTimeOpen{{ a }}" class="w-full bg-white rounded-lg border border-gray-300 focus:border-yellow-500 focus:ring-2 focus:ring-yellow-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                                                    </div>
                                                </div>
                                                <div class="col-span-2 sm:col-span-1">
                                                    <label for="lundiTimeClose{{ a }}" class="text-black">Fermeture</label>
                                                    <div class="flex rounded-md shadow-sm">
                                                        <input type="time" name="lundiTimeClose{{ a }}" {% if openingHour.end %}value="{{ openingHour.end |format_time(locale='fr') }}"{% endif%} id="lundiTimeClose{{ a }}" class="w-full bg-white rounded-lg border border-gray-300 focus:border-yellow-500 focus:ring-2 focus:ring-yellow-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                                                    </div>
                                                </div>
                                            {% endif %}
                                            {% set a = a + 1 %}
                                        {% endfor %}
                                    </div>
                                    <div class="grid grid-cols-2 gap-2 gap-y-1 hidden" id="mardi">
                                        <div class="col-span-2 flex flex-row">
                                            <label class="block text-base font-semibold text-black">
                                                Mardi
                                            </label>
                                        </div>
                                        {% set b = 1 %}
                                        {% for openingHour in openingHours %}
                                            {% if openingHour.day == 2 %}
                                                <div class="col-span-2 sm:col-span-1">
                                                    <label for="mardiTimeOpen{{ b }}" class="text-black">Ouverture</label>
                                                    <div class="flex rounded-md shadow-sm">
                                                        <input type="time" name="mardiTimeOpen{{ b }}" {% if openingHour.start %}value="{{ openingHour.start | format_time(locale='fr')}}"{% endif%} id="mardiTimeOpen{{ b }}" class="w-full bg-white rounded-lg border border-gray-300 focus:border-yellow-500 focus:ring-2 focus:ring-yellow-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                                                    </div>
                                                </div>
                                                <div class="col-span-2 sm:col-span-1">
                                                    <label for="mardiTimeClose{{ b }}" class="text-black">Fermeture</label>
                                                    <div class="flex rounded-md shadow-sm">
                                                        <input type="time" name="mardiTimeClose{{ b }}" {% if openingHour.end %}value="{{ openingHour.end | format_time(locale='fr')}}"{% endif%} id="mardiTimeClose{{ b }}" class="w-full bg-white rounded-lg border border-gray-300 focus:border-yellow-500 focus:ring-2 focus:ring-yellow-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                                                    </div>
                                                </div>
                                            {% endif %}
                                            {% set b = b + 1 %}
                                        {% endfor %}
                                    </div>
                                    <div class="grid grid-cols-2 gap-2 gap-y-1 hidden" id="mercredi">
                                        <div class="col-span-2 flex flex-row">
                                            <label class="block text-base font-semibold text-black">
                                                Mercredi
                                            </label>
                                        </div>
                                        {% set c = 1 %}
                                        {% for openingHour in openingHours %}
                                            {% if openingHour.day == 3 %}
                                                <div class="col-span-2 sm:col-span-1">
                                                    <label for="mercrediTimeOpen{{ c }}" class="text-black">Ouverture</label>
                                                    <div class="flex rounded-md shadow-sm">
                                                        <input type="time" name="mercrediTimeOpen{{ c }}" {% if openingHour.start %}value="{{ openingHour.start | format_time(locale='fr')}}"{% endif%} id="mercrediTimeOpen{{ c }}" class="w-full bg-white rounded-lg border border-gray-300 focus:border-yellow-500 focus:ring-2 focus:ring-yellow-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                                                    </div>
                                                </div>
                                                <div class="col-span-2 sm:col-span-1">
                                                    <label for="mercrediTimeClose{{ c }}" class="text-black">Fermeture</label>
                                                    <div class="flex rounded-md shadow-sm">
                                                        <input type="time" name="mercrediTimeClose{{ c }}" {% if openingHour.end %}value="{{ openingHour.end | format_time(locale='fr')}}"{% endif%} id="mercrediTimeClose{{ c }}" class="w-full bg-white rounded-lg border border-gray-300 focus:border-yellow-500 focus:ring-2 focus:ring-yellow-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                                                    </div>
                                                </div>
                                            {% endif %}
                                            {% set c = c + 1 %}
                                        {% endfor %}
                                    </div>
                                    <div class="grid grid-cols-2 gap-2 gap-y-1 hidden" id="jeudi">
                                        <div class="col-span-2 flex flex-row">
                                            <label class="block text-base font-semibold text-black">
                                                Jeudi
                                            </label>
                                        </div>
                                        {% set d = 1 %}
                                        {% for openingHour in openingHours %}
                                            {% if openingHour.day == 4 %}
                                                <div class="col-span-2 sm:col-span-1">
                                                    <label for="jeudiTimeOpen{{ d }}" class="text-black">Ouverture</label>
                                                    <div class="flex rounded-md shadow-sm">
                                                        <input type="time" name="jeudiTimeOpen{{ d }}" {% if openingHour.start %}value="{{ openingHour.start | format_time(locale='fr')}}"{% endif%} id="jeudiTimeOpen{{ d }}" class="w-full bg-white rounded-lg border border-gray-300 focus:border-yellow-500 focus:ring-2 focus:ring-yellow-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                                                    </div>
                                                </div>
                                                <div class="col-span-2 sm:col-span-1">
                                                    <label for="jeudiTimeClose{{ d }}" class="text-black">Fermeture</label>
                                                    <div class="flex rounded-md shadow-sm">
                                                        <input type="time" name="jeudiTimeClose{{ d }}" {% if openingHour.end %}value="{{ openingHour.end | format_time(locale='fr')}}"{% endif%} id="jeudiTimeClose{{ d }}" class="w-full bg-white rounded-lg border border-gray-300 focus:border-yellow-500 focus:ring-2 focus:ring-yellow-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                                                    </div>
                                                </div>
                                            {% endif %}
                                            {% set d = d + 1 %}
                                        {% endfor %}
                                    </div>
                                    <div class="grid grid-cols-2 gap-2 gap-y-1 hidden" id="vendredi">
                                        <div class="col-span-2 flex flex-row">
                                            <label class="block text-base font-semibold text-black">
                                                Vendredi
                                            </label>
                                        </div>
                                        {% set e = 1 %}
                                        {% for openingHour in openingHours %}
                                            {% if openingHour.day == 5 %}
                                                <div class="col-span-2 sm:col-span-1">
                                                    <label for="vendrediTimeOpen{{ e }}" class="text-black">Ouverture</label>
                                                    <div class="flex rounded-md shadow-sm">
                                                        <input type="time" name="vendrediTimeOpen{{ e }}" {% if openingHour.start %}value="{{ openingHour.start | format_time(locale='fr')}}"{% endif%} id="vendrediTimeOpen{{ e }}" class="w-full bg-white rounded-lg border border-gray-300 focus:border-yellow-500 focus:ring-2 focus:ring-yellow-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                                                    </div>
                                                </div>
                                                <div class="col-span-2 sm:col-span-1">
                                                    <label for="vendrediTimeClose{{ e }}" class="text-black">Fermeture</label>
                                                    <div class="flex rounded-md shadow-sm">
                                                        <input type="time" name="vendrediTimeClose{{ e }}" {% if openingHour.end %}value="{{ openingHour.end | format_time(locale='fr')}}"{% endif%} id="vendrediTimeClose{{ e }}" class="w-full bg-white rounded-lg border border-gray-300 focus:border-yellow-500 focus:ring-2 focus:ring-yellow-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                                                    </div>
                                                </div>
                                            {% endif %}
                                            {% set e = e + 1 %}
                                        {% endfor %}
                                    </div>
                                    <div class="grid grid-cols-2 gap-2 gap-y-1 hidden" id="samedi">
                                        <div class="col-span-2 flex flex-row">
                                            <label class="block text-base font-semibold text-black">
                                                Samedi
                                            </label>
                                        </div>
                                        {% set f = 1 %}
                                        {% for openingHour in openingHours %}
                                            {% if openingHour.day == 6 %}
                                                <div class="col-span-2 sm:col-span-1">
                                                    <label for="samediTimeOpen{{ f }}" class="text-black">Ouverture</label>
                                                    <div class="flex rounded-md shadow-sm">
                                                        <input type="time" name="samediTimeOpen{{ f }}" {% if openingHour.start %}value="{{ openingHour.start | format_time(locale='fr')}}"{% endif%} id="samediTimeOpen{{ f }}" class="w-full bg-white rounded-lg border border-gray-300 focus:border-yellow-500 focus:ring-2 focus:ring-yellow-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                                                    </div>
                                                </div>
                                                <div class="col-span-2 sm:col-span-1">
                                                    <label for="samediTimeClose{{ f }}" class="text-black">Fermeture</label>
                                                    <div class="flex rounded-md shadow-sm">
                                                        <input type="time" name="samediTimeClose{{ f }}" {% if openingHour.end %}value="{{ openingHour.end | format_time(locale='fr')}}"{% endif%} id="samediTimeClose{{ f }}" class="w-full bg-white rounded-lg border border-gray-300 focus:border-yellow-500 focus:ring-2 focus:ring-yellow-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                                                    </div>
                                                </div>
                                            {% endif %}
                                            {% set f = f + 1 %}
                                        {% endfor %}
                                    </div>
                                    <div class="grid grid-cols-2 gap-2 gap-y-1 hidden" id="dimanche">
                                        <div class="col-span-2 flex flex-row">
                                            <label class="block text-base font-semibold text-black">
                                                Dimanche
                                            </label>
                                        </div>
                                        {% set i = 1 %}
                                        {% for openingHour in openingHours %}
                                            {% if openingHour.day == 7 %}
                                                <div class="col-span-2 sm:col-span-1">
                                                    <label for="dimancheTimeOpen{{ i }}" class="text-black">Ouverture</label>
                                                    <div class="flex rounded-md shadow-sm">
                                                        <input type="time" name="dimancheTimeOpen{{ i }}" {% if openingHour.start %}value="{{ openingHour.start | format_time(locale='fr')}}"{% endif%} id="dimancheTimeOpen{{ i }}" class="w-full bg-white rounded-lg border border-gray-300 focus:border-yellow-500 focus:ring-2 focus:ring-yellow-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                                                    </div>
                                                </div>
                                                <div class="col-span-2 sm:col-span-1">
                                                    <label for="dimancheTimeClose{{ i }}" class="text-black">Fermeture</label>
                                                    <div class="flex rounded-md shadow-sm">
                                                        <input type="time" name="dimancheTimeClose{{ i }}" {% if openingHour.end %}value="{{ openingHour.end | format_time(locale='fr')}}"{% endif%} id="dimancheTimeClose{{ i }}" class="w-full bg-white rounded-lg border border-gray-300 focus:border-yellow-500 focus:ring-2 focus:ring-yellow-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                                                    </div>
                                                </div>
                                            {% endif %}
                                            {% set i = i + 1 %}
                                        {% endfor %}
                                    </div>
                                </div>
                                <input type="hidden" value="1" name="day">
                                <div class="px-4 py-3 bg-white text-right sm:px-6">
                                    <button type="submit" class="inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-base font-semibold rounded-md text-white bg-yellow-500 hover:bg-yellow-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-yellow-500 focus:ring-offset-white">
                                        Sauvegarder les horaires
                                    </button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
            <div class="block" aria-hidden="true">
                <div class="py-5">
                    <div class="border-t border-gray-200"></div>
                </div>
            </div>
            <div class="mt-0">
                <div class="md:grid md:grid-cols-3 md:gap-6">
                    <div class="md:col-span-1">
                        <div class="px-4 sm:px-0">
                            <h3 class="text-2xl font-bold uppercase text-black inline-flex items-center">
                                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7a4 4 0 11-8 0 4 4 0 018 0zM9 14a6 6 0 00-6 6v1h12v-1a6 6 0 00-6-6zM21 12h-6" />
                                </svg>
                                <span class="ml-1">Supprimer votre boutique</span>
                            </h3>
                        </div>
                    </div>
                    <div class="mt-5 md:mt-0 md:col-span-2">
                        {{ form_start(form_delete) }}
                        <div class="shadow-2xl overflow-hidden rounded-lg">
                            <div class="px-4 py-5 bg-white space-y-6 sm:p-6">
                                <div class="text-center flex items-center flex-col">
                                    <p class="mt-2 text-black font-bold">
                                        Vous n'êtes pas satisfait du contenu proposé sur le site ?
                                        <br>
                                        Ou vous souhaitez supprimer toutes les informations associées à votre boutique ?
                                    </p>
                                    <p class="mt-2 text-black font-bold">
                                        Cette fonctionnalit&eacute; va supprimer toutes les donn&eacute;es relatives à votre boutique,
                                        ainsi que toutes les commandes passées sur le site qui ne sont encore livrées
                                        (donc même celles en attente et en préparation).
                                    </p>
                                    {{ form_row(form_delete.id) }}
                                </div>
                            </div>
                            <div class="px-4 py-3 bg-white text-right sm:px-6">
                                <button type="submit" class="inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-base font-semibold rounded-md text-white bg-red-500 hover:bg-red-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 focus:ring-offset-white">
                                    Supprimer votre boutique
                                </button>
                            </div>
                        </div>
                        {{ form_end(form_delete) }}
                    </div>
                </div>
            </div>
        </div>
    </section>
    <script>
        function openModal(modalID) {
            document.getElementById(modalID).classList.toggle('hidden');
            document.getElementById(modalID).classList.toggle('fixed');
        }

        function changeTab(divID, tabID) {
            document.getElementById(divID).classList.toggle('hidden');
            document.getElementById(divID).classList.toggle('block');
            document.getElementById(tabID).classList.toggle('tab-active');
        }

        function addHoraire(divID, buttonID) {
            document.getElementById(buttonID).classList.add('hidden');
            let div = document.createElement('div');
            div.classList = 'col-span-2 sm:col-span-1';
            div.id = `test-${divID}`;
            div.innerHTML = `
                <label>Ouverture</label>
                <div class="flex rounded-md shadow-sm">
                    <input type="time" name="test-${divID}3" class="w-full bg-white rounded-lg border border-gray-300 focus:border-yellow-500 focus:ring-2 focus:ring-yellow-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                </div>
            `;
            document.getElementById(divID).appendChild(div);
            let div2 = document.createElement('div');
            div2.classList = 'col-span-2 sm:col-span-1';
            div2.innerHTML = `
                <label>Fermeture</label>
                <div class="flex rounded-md shadow-sm">
                    <input type="time" name="test-${divID}4" class="w-full bg-white rounded-lg border border-gray-300 focus:border-yellow-500 focus:ring-2 focus:ring-yellow-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                </div>
            `;
            document.getElementById(divID).appendChild(div2);
            let del = document.createElement('div');
            del.classList = 'col-span-2 flex flex-row items-center justify-center';
            del.id = divID + '-delete';
            del.innerHTML =`<button type="button" onclick="suppHoraire('${divID}','${buttonID}')" class="px-3 py-2 dark:text-white text-black bg-yellow-600 mt-1 rounded-lg">Supprimer des horaires</button>`;
            document.getElementById(divID).appendChild(del);
        }
        function suppHoraire(divID, buttonID) {
            document.getElementById(divID).lastChild.remove();
            document.getElementById(divID).lastChild.remove();
            document.getElementById(divID).lastChild.remove();
            document.getElementById(buttonID).classList.remove('hidden');
        }
    </script>
    {% include 'footer.html.twig' %}
{% endblock %}
