<nav class="navbar navbar-expand-md">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-nav" aria-controls="navbar-nav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbar-nav">
            <ul class="navbar-nav">
                <li class="nav-item link-home">
                    <a class="nav-link active" href="{{ path('home') }}"><i class="bi-house-fill"></i></a>
                </li>
                <li class="nav-item link-shop">
                    <a class="nav-link" href="/boutiques"><i class="bi-shop-window"></i> Boutiques</a>
                </li>
                <li class="nav-item link-products">
                    <a class="nav-link" href="/produits"><i class="bi-box-seam"></i> Produits</a>
                </li>
                {% if user is defined and user != null and is_granted('ROLE_MERCHANT') %}
                <li class="nav-item link-products">
                    <a class="nav-link" href="/ma-boutique"><i class="bi-shop"></i> Ma boutique</a>
                </li>
                {% endif %}
            </ul>
            <div class="navbar-end">
                <li class="nav-item">
                    <a class="nav-link" href="/recherche"><i class="bi-search"></i> Recherche</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle show" id="dropdown-user" data-bs-toggle="dropdown" aria-expanded="true"><i class="bi-person"></i>
                        {% if user is defined and user != null and is_granted('ROLE_ADMIN') %}
                            {{ user.username }} <span class="admin-badge badge-header"><i class="fad fa-badge-check"></i></span>
                        {% elseif user is defined and user != null and is_granted('ROLE_USER') %}
                            {{ user.username }}
                        {% else %}
                        Mon compte
                        {% endif %}
                    </a>
                    <ul class="dropdown-menu dropdown-menu-dark text-center show dropdown-menu-end dropdown-menu-lg-start dropdown-user" aria-labelledby="dropdown-user">
                        {% if user is defined and user != null and is_granted('ROLE_ADMIN') %}
                        <li><a class="dropdown-item" href="/admin"><i class="bi-shield-lock"></i> Administration</a></li>
                        <li><a class="dropdown-item" href="/profil"><i class="bi-person-lines-fill"></i> Mon Profil</a></li>
                        <li><a class="dropdown-item" href="/commandes"><i class="bi-bag-check"></i> Mes commandes</a></li>
                        <li><a class="dropdown-item" href="/deconnexion"><button class="btn btn-danger btn-no-mt"><i class="bi-box-arrow-right"></i> Déconnexion</button></a></li>
                        {% elseif user is defined and user != null and is_granted('ROLE_USER') %}
                        <li><a class="dropdown-item" href="/profil"><i class="bi-person-lines-fill"></i> Mon Profil</a></li>
                        <li><a class="dropdown-item" href="/commandes"><i class="bi-bag-check"></i> Mes commandes</a></li>
                        <li><a class="dropdown-item" href="/deconnexion"><button class="btn btn-danger btn-no-mt"><i class="bi-box-arrow-right"></i> Déconnexion</button></a></li>
                        {% else %}
                        <li><a class="dropdown-item" href="/connexion"><i class="bi-box-arrow-in-right"></i> Connexion</a></li>
                        <li><a class="dropdown-item" href="/inscription"><i class="bi-person-plus"></i> Inscription</a></li>
                        {% endif %}
                    </ul>
                </li>
            </div>
        </div>
    </div>
</nav>